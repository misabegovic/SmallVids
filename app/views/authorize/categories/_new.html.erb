<%= form_for(@category, html: {multipart: true, method: :put }) do |f| %>
	<div class="input-group">
	  <%= f.text_field :name, class: "form-control", placeholder: "Add new Folder", remote: true %>
    <button class="btn btn-secondary btn-sm", remote="true", name="commit", type="submit">Save</button>
  </div>
  <br/>
<%end%>