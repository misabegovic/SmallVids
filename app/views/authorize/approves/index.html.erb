<div class="container">
	<div class="row">
		<div class="col">
			<% if @videos.empty? %>
				<h4>No videos for approval</h4>
			<%else%>
				<div id="show_videos">
					<%= render @videos%>
				</div>
			<%end%>
		</div>
	</div>
</div>
